import React from 'react';
import style from './Header.scss';

const Header = () => {
  return (
    <header>
      <h1 className={style.title}>Title For Things</h1>
    </header>
  );
};

export default Header;
